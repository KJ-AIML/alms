"""ALMS Phase 0 Runtime Audit Lab harness.

Research/audit infrastructure ONLY. Nothing in this package is the ALMS runtime,
and the Probe Process Protocol v0 is NOT the future ModelRuntime contract.

P0.1 scope: evidence schemas + schema loading/validation helpers + a read-only CLI
(`validate`, `list-fixtures`, `list-lanes`). No provider SDKs, no network code.
"""

__version__ = "0.0.0"
