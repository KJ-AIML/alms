# P0.7D-harness-cli — Missing Harness CLI Commands

Status: offline CLI completion only. No live provider request was made during
P0.7D-harness-cli. No production ALMS code was modified. P0.8 Stress Semantics
implementation was not begun.

Authority: DevSpec Section 38; P0.7D-invariants (`evaluate_invariants`); autonomous
offline roadmap.

## Goal

Add the four remaining Section 38 commands that operate on recorded artifacts:

```text
alms-audit normalize --run <RUN_ID>
alms-audit evaluate --run <RUN_ID>
alms-audit summarize --run <RUN_ID>
alms-audit verify-evidence --revision <REV>
```

## Behavior

| Command | Behavior |
| --- | --- |
| `normalize` | Re-reads `probe-response.json` + raw response; rewrites normalized transcript + result. Skips fixtures without raw evidence. |
| `evaluate` | Runs `evaluate_invariants` per fixture; writes `invariant-results.json` and `evaluate-report.json`. |
| `summarize` | Prints `run-summary.json` fields (mode, lane, model, fixture statuses, secret_scan). |
| `verify-evidence` | Verifies `evidence/<revision>/` presence and optional `manifest.json` hashes. Honest `NOT_FOUND` for absent P0-E1 (exit 2). |

All four are offline. No credentials. No network.

## Files

| Path | Role |
| --- | --- |
| `harness/src/alms_audit/postprocess.py` | Shared post-run helpers |
| `harness/src/alms_audit/cli.py` | Wire four commands |
| `harness/tests/test_cli_postprocess.py` | CLI + helper coverage |
| `docs/P0.7D_HARNESS_CLI.md` | This report |

## Gate / finding preservation

| Item | Status |
| --- | --- |
| G2 | blocked_credential (unchanged) |
| G3 | blocked_credential (unchanged) |
| G7 | not_started — `verify-evidence --revision P0-E1` reports NOT_FOUND |
| L-01 | F2 / proposed (unchanged) |
| Readiness | READY_AFTER_LIVE_GATES |
| Phase 0 | not complete |
| P0.8 | not authorized |

## Commands

From `harness/`:

```bash
uv sync --frozen
uv run ruff check .
uv run ruff format --check .
uv run pytest tests/test_cli_postprocess.py tests/test_cli.py -q
uv run alms-audit verify-evidence --revision P0-E1
```

## Next eligible slice

`P0.9-q005-deferral` — explicit EmbeddingRuntime deferral (EXIT-12).
