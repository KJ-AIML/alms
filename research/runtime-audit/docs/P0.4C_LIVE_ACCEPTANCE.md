# P0.4C First OpenAI Native Live Acceptance Report

Terminal outcome: **BLOCKED_CREDENTIAL** (blocked by an unavailable direct OpenAI
credential, not a failure). Zero provider requests were attempted; no live OpenAI call was
made. All offline gates and the snapshot-pinning change passed and are committed.

## 1. Published P0.4B Commit

`3fc509c` ("feat(audit): wire bounded openai live runner") is present on
`origin/research/runtime-audit-phase0`. Verified at session start: local == remote at
`3fc509c`; working tree clean; all three repos (`alms`, `agent-native-backend`,
`alms-langgraph-agent-skill`) clean. The commit's 23 files are all under
`research/runtime-audit/`; both `harness/uv.lock` and `probes/openai-native/uv.lock` are
tracked; `runs/` tracks only `.gitkeep`; no untracked live evidence existed.

## 2. Snapshot-Pinning Commit

`2299496` ("chore(audit): pin first-live openai model snapshot"), 9 files changed. It pins
the first-live model from the moving alias `gpt-5.4-nano` to the exact snapshot
`gpt-5.4-nano-2026-03-17` across the first-live pricing snapshot `model_id`, the
planner/probe/manifest model, and the coupled tests and docs. No source code (`.py` under
`src/`) and no production ALMS files changed. This commit is currently local-only (see
section 21): the tool-layer `git push` is Heli-Harness guardrailed and must be run by the
operator.

## 3. Repository State Before Execution

Branch `research/runtime-audit-phase0`. Working tree clean before Stage 2. Target repo
`alms` matches `.heli-harness/workspace/target.json`. All changes this session are additive
and confined to `research/runtime-audit/`.

## 4. Official Model and Pricing Reconfirmation

Reconfirmed against `developers.openai.com` (model + pricing docs) on 2026-07-13.

| Attribute | Official value | Committed snapshot | Agreement |
|---|---|---|---|
| Snapshot ID | `gpt-5.4-nano-2026-03-17` (released 2026-03-17) | same | exact |
| Responses API | Supported (`v1/responses`) | n/a | yes |
| Text generation | Supported | n/a | yes |
| Streaming | Supported | n/a | yes |
| Function calling | Supported | n/a | yes |
| Structured outputs | Supported | n/a | yes |
| Input / 1M | $0.20 | $0.20 | exact |
| Cached input / 1M | $0.02 | recorded in note only | additive |
| Output / 1M | $1.25 | $1.25 | exact |
| Context / max output | 400,000 / 128,000 | max_output_tokens 128 | within |

Committed input/output prices already matched the official values, so no pricing correction,
no recalculation, and no re-sync were required. Cached-input ($0.02/1M) is recorded for
completeness; it is intentionally not modelled in the snapshot schema because the upper
bound uses the full input price (more conservative) with retries=0, so caching cannot raise
the bound. The committed pricing snapshot remains the execution source of truth; pricing was
not fetched dynamically during any execution.

## 5. Credential Presence

`OPENAI_API_KEY present: false`

Only presence was checked. The value was not printed, inspected, prefixed, measured, hashed,
serialized, or placed in any diagnostic.

## 6. Final Immutable Plan (built, not executed)

```
Lane:     openai-native
Model:    gpt-5.4-nano-2026-03-17
Fixtures: GEN-001, ROLE-001, STR-001, TOOL-001, STREAM-001, USAGE-001
Excluded: ERROR-001, TOOL-004, STREAM-002, CANCEL-001, TIMEOUT-001, RETRY-001,
          USAGE-002, EMBED-001, MM-001

expected provider calls: 6
maximum provider calls:  8
retries:                 0
hard budget cap:         1.00 USD
soft budget cap:         0.25 USD
max output tokens/fixt.: <= 128
upper-bound cost (6):    0.00156 USD
one subprocess + one provider request per fixture
```

The plan was not executed: the credential-presence gate fails closed before any run is
created, so no run ID was minted and no subprocess was launched.

## 7. Exact Command Executed

None. No live command was executed. The command that a future authorized session would run
(only with a real direct `OPENAI_API_KEY` present) is:

```
alms-audit run \
  --config ../configs/first-live.override.toml \
  --lane openai-native \
  --model gpt-5.4-nano-2026-03-17 \
  --fixtures GEN-001,ROLE-001,STR-001,TOOL-001,STREAM-001,USAGE-001 \
  --run-id <NEW_UNIQUE_RUN_ID> \
  --live \
  --confirm-live
```

## 8. Provider Request Count

0 provider requests attempted. 0 retries.

## 9. Fixture Results

No fixture reached the provider. Each of the six approved fixtures is classified
`BLOCKED_CREDENTIAL` (no live attempt). For reference, an offline mock simulation of the
same six fixtures through the real CLI and real subprocess (0 network calls) produced
terminal statuses PASS / PASS / PASS / PASS / PASS_WITH_EXTENSION / PASS; that is pipeline
evidence in `offline_mock` mode, explicitly not live evidence, and its ephemeral run
directory was removed.

## 10. Structured Output Evidence

None. Native structured-output strict-mode semantics remain `unverified_until_live`.

## 11. Tool-Call Evidence

None. Real function tool-call semantics and argument fidelity remain `unverified_until_live`.

## 12. Streaming Evidence

None. Real streaming event ordering and terminal state remain `unverified_until_live`.

## 13. Usage Evidence

None. Provider-reported usage shape and values remain `unverified_until_live`. No usage was
coerced to zero; absent stays absent.

## 14. Provider Extensions

None observed (no live response was received).

## 15. Raw and Normalized Evidence Layout

No live evidence was written. The layout a live run would populate (raw before normalized,
per fixture) is: `runs/<run-id>/run-manifest.json`, `runs/<run-id>/run-summary.json`, and
`runs/<run-id>/fixtures/<FIXTURE>/{raw/openai_*.json, probe-response.json,
normalized-transcript.json, result.json}`. Tracked `runs/` still contains only `.gitkeep`.

## 16. Secret Safety Results

- Credential value never read, printed, serialized, or diagnosed; only a presence boolean.
- Tracked-secret scan of `research/runtime-audit/` clean; the only `sk-` tokens are labelled
  synthetic fixtures used to test redaction and the secret scanner.
- Offline simulation run-level secret scan: `clean`.
- Provider-SDK isolation: the harness cannot import `openai` (`find_spec` is `None`).
- No live request metadata or secret-bearing headers were produced (no live request).

## 17. Cost Estimate

- Planned upper-bound cost: 0.00156 USD (6 calls); 0.00208 USD at the 8-call cap.
- Estimated cost from provider-reported usage: not applicable (no live usage).
- No billed cost. The upper bound is a safety estimate, not an accounting or billed figure.

## 18. Unexpected Findings

None. The environment lacks a direct OpenAI credential exactly as anticipated by the
credential clarification; every gate behaved fail-closed as designed.

## 19. Finding Severity Assessment

- F0 (strategy-invalidating): none.
- F1 (contract-changing): none. This run legitimately produced no F1.
- F2 (capability-shaping): none.
- F3 (implementation note): none.
- F4 (environmental): the execution environment has no direct `OPENAI_API_KEY`; only an
  OpenRouter or other custom-base-URL credential may be available, which must never be used
  as OpenAI Native evidence.

## 20. Validation Results

- Harness: `uv sync --frozen` ok; `ruff check .` ok; `ruff format --check .` ok (31 files);
  `pytest` 114 passed; `alms-audit validate` PASS.
- Probe (openai-native): `uv sync --frozen` ok; `ruff check .` ok; `ruff format --check .`
  ok (15 files); `pytest` 29 passed.
- Offline six-fixture simulation with the pinned snapshot: 0 provider calls, 0 SDK requests;
  every probe response `execution_mode = offline_mock`; run manifest `execution_mode =
  offline`, model `gpt-5.4-nano-2026-03-17`, `estimated_upper_bound_cost_usd = 0.00156`,
  `secret_scan = clean`, `credential_presence.OPENAI_API_KEY = false`.
- U+00A7 scan of tracked research files and harness state: clean.

## 21. Git Status

- Branch `research/runtime-audit-phase0`; working tree clean.
- Local HEAD `2299496`; `origin/research/runtime-audit-phase0` at `3fc509c`; local ahead by 1
  plus this report commit. Publishing is pending: the tool-layer `git push` is Heli-Harness
  guardrailed, so the operator must run
  `cd repos/alms && git push origin research/runtime-audit-phase0`.
- All three repos clean; no untracked files outside gitignored caches/venvs.

## 22. Live Acceptance Completion Status

BLOCKED_CREDENTIAL. Provider requests attempted: 0. Retries attempted: 0. Recorded as
blocked by the unavailable direct OpenAI credential, not as failed. All completed offline
evidence and published commits are preserved. No OpenRouter or custom-base-URL substitution
was made; the OpenAI Native probe base URL was not changed; the credential gate was not
weakened, bypassed, renamed, or reinterpreted.

## 23. Recommendation: proceed to P0.5 or investigate

Do not proceed to P0.5. The first OpenAI Native live acceptance is not yet satisfied because
no direct OpenAI credential was available to execute the single bounded run. Choose one
future path (neither implemented here):

1. **Resume OpenAI Native live acceptance later (recommended)** when a direct OpenAI API key
   is available in the execution environment. Everything else is ready: the plan is pinned
   and immutable, the offline pipeline is proven, and the gates are fail-closed.
2. **Design a separate OpenRouter gateway lane** with its own lane identity, configuration,
   model slugs, pricing evidence, raw semantics, capability matrix, and conformance results.
   An OpenRouter or custom-base-URL run must never be labelled as OpenAI Native evidence.
