{
  "spec": "alms.dev/runtime-audit-fixture/v0",
  "id": "STR-005",
  "feature": "structured_output",
  "tier": "STRESS",
  "cost_class": "live_stress",
  "intent": "Ambiguous input for a required field should reveal hidden framework retries or parser loops rather than silently resolving.",
  "summary": "Validation ambiguity",
  "messages": [
    { "role": "user", "parts": [{ "kind": "text", "text": "The meeting is at 3." }] }
  ],
  "output_schema": {
    "type": "object",
    "required": ["time", "timezone"],
    "properties": { "time": { "type": "string" }, "timezone": { "type": "string" } },
    "additionalProperties": false
  },
  "requirements": {
    "live_required": true,
    "supports_mock_mode": false,
    "capabilities": ["generation", "structured_output"],
    "provider_extension_allowed": true
  },
  "execution": { "temperature": 0, "max_output_tokens": 64, "timeout_ms": 30000, "stream": false, "max_attempts": 1 },
  "expected_invariants": { "retry_count_observed": true, "validation_location_recorded": true },
  "allowed_outcomes": ["PASS", "PASS_WITH_EXTENSION", "INCONCLUSIVE", "FAIL_INVARIANT"],
  "evidence_requirements": {
    "raw": ["raw_response", "outbound_attempts"],
    "normalized": ["structured_output_completed", "provider_extension"]
  }
}
