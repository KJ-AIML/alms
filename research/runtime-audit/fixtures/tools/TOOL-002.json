{
  "spec": "alms.dev/runtime-audit-fixture/v0",
  "id": "TOOL-002",
  "feature": "tools",
  "tier": "BASE",
  "cost_class": "live_core",
  "intent": "A forced/required tool choice; record how the runtime represents tool-choice control.",
  "summary": "Forced or required tool call",
  "messages": [
    { "role": "user", "parts": [{ "kind": "text", "text": "Look up the weather in Tokyo." }] }
  ],
  "tools": [
    {
      "name": "get_weather",
      "description": "Return the current weather for a city.",
      "parameters": {
        "type": "object",
        "required": ["city"],
        "properties": { "city": { "type": "string" } },
        "additionalProperties": false
      }
    }
  ],
  "requirements": {
    "live_required": true,
    "supports_mock_mode": false,
    "capabilities": ["generation", "tools"],
    "accepted_capability_modes": ["required", "forced"]
  },
  "execution": { "temperature": 0, "max_output_tokens": 128, "timeout_ms": 30000, "stream": false, "max_attempts": 1, "tool_choice": "required" },
  "expected_invariants": { "tool_call_count": 1, "expected_tool_name": "get_weather", "tool_choice_respected": true },
  "allowed_outcomes": ["PASS", "PASS_WITH_EXTENSION", "UNSUPPORTED_OBSERVED"],
  "evidence_requirements": {
    "raw": ["raw_response", "raw_tool_arguments"],
    "normalized": ["tool_call_started", "tool_call_completed"]
  }
}
